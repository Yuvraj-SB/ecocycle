spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/marketplace
    username: ecocycle
    password: ecocycle
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  main:
    allow-bean-definition-overriding: true
server:
  port: 8083

