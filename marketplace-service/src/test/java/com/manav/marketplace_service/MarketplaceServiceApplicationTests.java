package com.manav.marketplace_service;

import org.junit.jupiter.api.Test;

/** Basic test to verify Marketplace Service compiles. */
class MarketplaceServiceApplicationTests {

  @Test
  void contextLoads() {
    // Minimal test - just verifies the test class compiles and runs
    // Full Spring context tests would require database configuration
    assert true;
  }
}
