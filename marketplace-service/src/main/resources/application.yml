spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://marketplace-db:5432/marketplace}
    username: ${SPRING_DATASOURCE_USERNAME:ecocycle}
    password: ${SPRING_DATASOURCE_PASSWORD:ecocycle}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8081

jwt:
  secret: my-super-long-secret-key-ecocycle-123456789   # keep same across services
  expiration: 86400000   # 1 day in ms